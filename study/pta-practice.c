#include <stdio.h>

int MaxSubseqSum2(int a[], int n);

int main(void)
{


    return 0;
}

int MaxSubseqSum2(int a[], int n)
{
    int ThisSum ,Maxsum;
    for (int i = 0; i < n; i++)
    {
        ThisSum = 0;
        for (int j = i; j < n; j++)
        {
            ThisSum += a[j];
            if ()
        }
    }
}
